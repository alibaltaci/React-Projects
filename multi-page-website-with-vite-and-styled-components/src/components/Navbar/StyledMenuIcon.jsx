import { styled } from "styled-components";

export const StyledMenuIcon = styled.div`

    /* display: none;
    
    svg[name="menu-outline"]{
        display: none;
    }

    @media ( max-width: ${({theme}) => theme.media.mobile} ) {
        
        display: inline-block;
        z-index: 999;
    } */

`
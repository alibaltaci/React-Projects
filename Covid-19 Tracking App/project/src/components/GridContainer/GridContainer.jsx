import { StyledGridContainer } from "./StyledGridContainer";

export default function GridContainer({children}) {
  return (
    <StyledGridContainer>
        {children}
    </StyledGridContainer>
  )
}
